{
  "rules": [
    {
      "AllowedOrigins": ["http://localhost:3000", "http://localhost:5173", "https://*.pages.dev", "https://*.e2b.dev"],
      "AllowedMethods": ["GET", "HEAD", "OPTIONS"],
      "AllowedHeaders": ["*"],
      "ExposeHeaders": ["Content-Type", "Content-Length", "ETag"],
      "MaxAgeSeconds": 3600
    }
  ]
}